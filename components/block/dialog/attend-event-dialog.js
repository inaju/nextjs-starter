"use client"

import { Button } from "@/components/ui/button"
import {
    Dialog,
    DialogContent,
    DialogDescription,
    DialogFooter,
    DialogHeader,
    DialogTitle,
    DialogTrigger,
} from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"

import { zodResolver } from "@hookform/resolvers/zod"
import { useForm } from "react-hook-form"

import {
    Form,
    FormControl,
    FormField,
    FormItem,
    FormLabel,
    FormMessage
} from "@/components/ui/form"
import { z } from "zod"
import { InputOTP, InputOTPGroup, InputOTPSlot } from "@/components/ui/input-otp"
import Image from "next/image"
import { PersonStanding } from "lucide-react"

export function AttendEvent({ event, eventOrganizer }) {
    const FormSchema = z.object({
        code: z.string().min(4, {
            message: "event code must be at least 4 characters.",
        }),
    })
    const form = useForm({
        resolver: zodResolver(FormSchema),
        defaultValues: {
            code: "",
        },
    })

    const handleSubmit = async (data) => {

    }
    console.log(eventOrganizer, 'event')

    async function onSubmit(data) {
        handleSubmit(data)
        console.log(data, 'data')
    };
    return (
        <Dialog>
            <DialogTrigger asChild>
                <Button variant="outline">
                    <PersonStanding size={18} />
                    Join</Button>
            </DialogTrigger>
            <DialogContent className="sm:max-w-[425px]">
                <DialogHeader>
                    <DialogTitle className="mb-6">Attend Event</DialogTitle>
                    <DialogDescription className="flex flex-col items-start">
                        Please Enter Event Code generated by
                        <div className="flex gap-2 items-center border-2 p-3 rounded-lg w-fit mt-2">
                            <Image src={eventOrganizer?.image} alt="event organizer" height={30} width={30} className="rounded-full border border-1 border-slate-600" />
                            <div className="text-slate-800 font-semibold">

                                {eventOrganizer?.name}
                            </div>
                        </div>
                    </DialogDescription>
                </DialogHeader>
                <Form {...form} >
                    <form onSubmit={form.handleSubmit(onSubmit)} className="mt-4">
                        <FormField
                            control={form.control}
                            name="code"
                            render={({ field }) => (
                                <FormItem>
                                    <FormLabel>Event Code</FormLabel>
                                    <FormControl>
                                        <InputOTP maxLength={4} {...field}>
                                            <InputOTPGroup>
                                                <InputOTPSlot index={0} />
                                                <InputOTPSlot index={1} />
                                                <InputOTPSlot index={2} />
                                                <InputOTPSlot index={3} />
                                            </InputOTPGroup>
                                        </InputOTP>
                                    </FormControl>
                                    <FormMessage />
                                </FormItem>
                            )}
                        />
                        <DialogFooter className="mt-4">
                            <Button type="submit">Submit</Button>
                        </DialogFooter>
                    </form>
                </Form>

            </DialogContent>
        </Dialog>
    )
}
